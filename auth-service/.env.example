# Auth.js secret – generate with: bun run gensecret (or: openssl rand -base64 32)
AUTH_SECRET=

# Base URL where this auth service is reachable
AUTH_URL=http://localhost:5173

# Keycloak OIDC provider
KEYCLOAK_ISSUER=http://localhost:8080/realms/shadcn-admin
KEYCLOAK_CLIENT_ID=shadcn-admin-web
KEYCLOAK_CLIENT_SECRET=

# Frontend SPA URL (for CORS and redirect)
APP_URL=http://localhost:5173

# Enable trust proxy when behind reverse proxy in production
AUTH_TRUST_HOST=true
